Tạo csdl trong mysql
- Tải mysql<Khi tải hãy nhớ tk mk bạn đã setup là gì> -> vào mysql workbench -> tạo 1 database -> click vào database đó để hiện lên giao diện code
- Lấy các đoạn code từ file "db_generator.sql", và thực hiện theo thứ tự từ đoạn code trên đến đoạn code dưới<để chạy code thì bôi đen đoan code rồi nhấn vào icon sấm sét ngoài cùng bên trái>
- Thực hiện các thao tác trên sẽ tạo ra thành công các bảng và dữ liệu trong csdl
